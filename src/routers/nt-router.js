/**
 * Router for connecting neural network service and routing requests from
 * @type {NeuralNetworkRouter}
 */
export class NeuralNetworkRouter{
    /**Connection object to neural network service class rabbitMQConnectionManager*/
    connection_NTS;
    /**
     * Creates a ConnectionManager object with methods for simplified routing of function execution requests and sending them
     */
    constructor(config) {
    }
    /**Creates routes on nts connection*/
    createRoutes(){
    }
}