/**
 * Router for connecting front side and routing requests from
 * @type {FrontRouter}
 */
export class FrontRouter{
    /**
     * Creates a ConnectionManager object with methods for simplified routing of function execution requests and sending them
     */
    constructor(config) {
    }
    /**Creates routes on connection*/
    createRoutes(){

    }
}